<div class="container">
  <h1><%= @post.title %></h2>

  <%tags = [] %>
  <% @post.tags.each do |tag| %>
      <%tags << tag.title.chomp %>
  <% end %>
  <p>
  	<%= tags.uniq.join(", ") %>
  <p>

  <p><%= @post.body %></p>

</div>